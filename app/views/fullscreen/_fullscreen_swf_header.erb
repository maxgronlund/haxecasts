<%= content_for :head do %>
  <script type="text/javascript">
    var flashvars = {
    };
    var params = {
      scale: "noScale",
      allowFullscreen: "true",
      allowScriptAccess: "always",
      bgcolor: "#888888"
    };
    var attributes = {
      id:"foo"
    };
    swfobject.embedSWF("<%=asset_path 'episode-09.swf' %>", "altContent", "100%", "100%", "10.0.0", "<%=asset_path 'expressInstall.swf' %>", flashvars, params, attributes);
  </script>
  <style>
    html, body { height:100%; }
    body { margin:0; }
    object { outline:none}
    #altContent{
    width: 100%;
    height: 100%;
    }
  </style>
<%end%>